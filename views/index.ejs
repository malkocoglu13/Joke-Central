<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles/main.css">
  <title>Joke Central</title>
  <link rel="stylesheet" href="/styles/main.css">
</head>

<body>
  <div class="box">
  <div class="container">
    <h1 class="typewriter">Joke Central: Your Daily Dose of Laughter</h1>
<form action="/joke" id="form" class="form" method="POST">
      <select name="category" class="form-select" id="category-select">
        <option value="Any" data-display="Select">Any Category</option>
        <option value="Programming">Programming</option>
        <option value="Miscellaneous">Misc</option>
        <option value="Dark">Dark</option>
        <option value="Pun">Pun</option>
        <option value="Spooky">Spooky</option>
        <option value="Christmas">Christmas</option>
      </select>
      <select name="language" class="form-select" id="language-select">
        <option value="">English</option>
        <option value="es">Spanish</option>
        <option value="fr">French</option>
        <option value="de">German</option>
        <option value="pt">Portuguese</option>
      </select>
      <select name="blacklist" class="form-select" id="blacklist-select">
        <option value="">No Blacklist</option>
        <option value="nsfw">Avoid Nsfw</option>
        <option value="religious">Avoid Religious</option>
        <option value="political">Avoid Political</option>
        <option value="racist">Avoid Racist</option>
        <option value="sexist">Avoid Sexist</option>
        <option value="explicit">Avoid Explicit</option>
      </select>
      <select name="joketype" class="form-select" id="joketype-select">
        <option value="">Any Type</option>
        <option value="single">Single Part</option>
        <option value="twopart">Two Part</option>
      </select>

      <button type="submit" class="form-submit">Go</button>
    </form>
    
    <section id="cards" class="cards">
        <%if (locals.data) {%>
          <!-- Display the API URL -->
  <div id="api-url" class="api-url">
    <% if (apiUrl) { %>
      <p>API URL: <%= apiUrl %></p>
    <% } else { %>
      <p>API URL not available</p>
    <% } %>
  </div>
            <article class="card-item">
              <h2 class="card-activity">
                <%=data.joke %>
                <%=data.setup %>
              </h2>
              <h2 class="card-activity">
                <%=data.delivery %>
              </h2>
            </article>
            <%}%>
              <%if (locals.error) {%>
                <div id="tag-error" class="tag-error">
                  <%=error%>
                </div>
                <%}%>
        </section>
      </div>
    </div>
    <script src="/js/form-persistence.js"></script>
    <footer>
      Made with ❤️ by Burak
    </footer>
    </body>
    
    </html>
